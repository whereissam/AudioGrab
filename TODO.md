# TODO

- [x] `to-video`: audio → YouTube-ready MP4 (still image + H.264 + AAC, faststart)
  - [ ] Follow-up: `--image` custom-image CLI flag (Python API already accepts `image`)
  - [ ] Follow-up: direct URL input for `to-video`
